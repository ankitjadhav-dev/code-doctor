import { AlertTriangle, CheckCircle2 } from 'lucide-react';
import { Badge } from '@/components/ui/badge';
import { Card, CardContent, CardHeader } from '@/components/ui/card';
import { ScoreRing } from '@/components/reports/score-ring';
import { categories, demoFindings, demoScore } from '@/features/reports/fixtures';

const severityClass = { HIGH: 'border-rose-200 bg-rose-50 text-rose-700 dark:border-rose-900 dark:bg-rose-950/40 dark:text-rose-300', MEDIUM: 'border-amber-200 bg-amber-50 text-amber-700 dark:border-amber-900 dark:bg-amber-950/40 dark:text-amber-300' };
export function ReportPreview() { return <div className="space-y-6"><section className="grid gap-5 rounded-2xl border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900 md:grid-cols-[auto_1fr]"><ScoreRing score={demoScore.overall} /><div><Badge className="mb-3 border-cyan-200 bg-cyan-50 text-cyan-700 dark:border-cyan-900 dark:bg-cyan-950/40 dark:text-cyan-300">Example report</Badge><h2 className="text-2xl font-semibold tracking-tight">Healthy foundation, focused opportunities</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-600 dark:text-slate-400">The score is calculated from deterministic evidence. AI commentary will interpret that evidence in a later phase; it never creates findings or scores.</p></div></section><section className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{categories.map((category) => <Card key={category.key}><CardContent className="flex items-center justify-between p-4"><div><p className="text-xs text-slate-500">{category.label}</p><p className="mt-1 text-xl font-semibold">{category.value}</p></div><div className="h-1.5 w-16 overflow-hidden rounded-full bg-slate-100 dark:bg-slate-800"><div className="h-full rounded-full bg-cyan-500" style={{ width: `${category.value}%` }} /></div></CardContent></Card>)}</section><Card><CardHeader><div className="flex items-center justify-between"><div><h3 className="font-semibold">Priority findings</h3><p className="mt-1 text-sm text-slate-500">Evidence-backed examples from a future scan.</p></div><AlertTriangle className="text-amber-500" size={20} /></div></CardHeader><CardContent className="space-y-3">{demoFindings.map((finding) => <div key={finding.id} className="rounded-lg border border-slate-200 p-4 dark:border-slate-800"><div className="flex flex-wrap items-center gap-2"><Badge className={severityClass[finding.severity as keyof typeof severityClass]}>{finding.severity}</Badge><span className="font-medium">{finding.title}</span></div><p className="mt-2 text-sm text-slate-600 dark:text-slate-400">{finding.description}</p><p className="mt-2 font-mono text-xs text-cyan-700 dark:text-cyan-300">{finding.file}:{finding.line}</p></div>)}<div className="flex items-center gap-2 pt-2 text-sm text-emerald-600 dark:text-emerald-400"><CheckCircle2 size={16} /> No critical issues in this illustrative report.</div></CardContent></Card></div>; }
